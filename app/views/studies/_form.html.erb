<%= form_for(@study) do |f| %>
  <% if @study.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@study.errors.count, "error") %> prohibited this study from being saved:</h2>

      <ul>
      <% @study.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= t "admin" %><br>
    <%= f.select :admin_id, options_for_select(@users.collect { |s| [s.email, s.id] }, selected: @study.admin_id), {} ,:class=>"reverse-dir" %>
  </div>
  <div class="field">
    <%= t "description" %><br>
    <%= f.text_area :description,:class=>"reverse-dir"%>
  </div>
  <div class="actions">
    <%= f.submit (t 'submit', :default => "Submit"), :class => "btn btn-default smooth-scroll"%>
  </div>
<% end %>
