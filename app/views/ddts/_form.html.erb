<%= form_for(@ddt) do |f| %>
  <% if @ddt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ddt.errors.count, "error") %> prohibited this ddt from being saved:</h2>

      <ul>
      <% @ddt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit (t 'submit', :default => "Submit"), :class => "btn btn-default smooth-scroll"%>
  </div>
<% end %>
