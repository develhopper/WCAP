<%= form_for(@clinic) do |f| %>
  <% if @clinic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clinic.errors.count, "error") %> prohibited this clinic from being saved:</h2>

      <ul>
      <% @clinic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= t 'clinic_name' %><br>
    <%= f.text_field :name,:class=>"reverse-dir" %>
  </div>
  <div class="field">
    <%= t 'supervisor' %><br>
    <%= f.select :supervisor_id, options_for_select(@users.collect { |s| [s.identity, s.id] }, selected: @clinic.supervisor_id), {} ,:class=>"reverse-dir" %>
  </div>
  <div class="actions">
    <%= f.submit (t 'submit', :default => "Submit"), :class => "btn btn-default smooth-scroll"%>
  </div>
<% end %>
