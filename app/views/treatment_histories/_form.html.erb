<%= form_for(@treatment_history) do |f| %>
  <% if @treatment_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@treatment_history.errors.count, "error") %> prohibited this treatment_history from being saved:</h2>

      <ul>
      <% @treatment_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :patient_id %><br>
    <%= f.text_field :patient_id %>
  </div>
  <div class="field">
    <%= f.label :is_cured %><br>
    <%= f.check_box :is_cured %>
  </div>
  <div class="field">
    <%= f.label :in_month_treated %><br>
    <%= f.check_box :in_month_treated %>
  </div>
  <div class="field">
    <%= f.label :in_year_treat_count %><br>
    <%= f.number_field :in_year_treat_count %>
  </div>
  <div class="field">
    <%= f.label :na_session %><br>
    <%= f.check_box :na_session %>
  </div>
  <div class="field">
    <%= f.label :total %><br>
    <%= f.number_field :total %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
